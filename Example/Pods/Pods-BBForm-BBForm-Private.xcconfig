#include "Pods-BBForm-BBForm.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/BBForm" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/BBForm" "${PODS_ROOT}/Headers/Expecta" "${PODS_ROOT}/Headers/Specta"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}